# Test dependencies for geo-dataset-readiness
# Install with: pip install -r tests/requirements-test.txt

# Core test framework
pytest==8.3.4
pytest-cov==6.0.0

# Property-based testing
hypothesis==6.122.3

# Required by the validator engine (same as runtime)
geopandas>=0.14.0
pyogrio>=0.7.0
shapely>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
